# Rust build output
/target/

# Cargo lock (keep for binaries)
# Cargo.lock  ← intentionally tracked

# Bundled Ghostscript DLL (redistribute separately due to AGPL)
gsdll64.dll
gsdll32.dll
vendor/gs/
installer/vendor/*.dll
installer/vendor/*.exe
# gs_lib/ is intentionally tracked (resource files needed by gswin64c.exe)

# Temp PDF files created at runtime
*.pdf
tmp/
temp/

# IDE / editor
.vscode/
.idea/
*.suo
*.user

# OS
Thumbs.db
Desktop.ini
.DS_Store
